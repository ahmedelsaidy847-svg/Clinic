# 🏥 عمل نسخة طبق الأصل لعيادة جديدة

السيستم مصمَّم بحيث تعمل منه **نسخ متعددة، كل عيادة منفصلة تماماً ببياناتها**.
الفكرة: **نفس الكود + قاعدة بيانات جديدة فاضية + إعدادات خاصة**. خطوة بخطوة:

---

## 1️⃣ نسخ الكود (GitHub)

افتح صفحة الريبو على GitHub، ثم إما:
- **الأسهل:** اضغط الزر الأخضر **"Use this template"** → **Create a new repository** → سمِّه باسم العيادة الجديدة (مثلاً `clinic-cairo`).
- أو **Fork** الريبو.

> النتيجة: ريبو جديد مستقل بنفس الكود بالكامل.

---

## 2️⃣ قاعدة بيانات جديدة فاضية (Neon)

1. ادخل **https://console.neon.tech**
2. اضغط **New Project** → سمِّه باسم العيادة الجديدة
3. بعد الإنشاء، من **Connection Details** انسخ رابطين:
   - **Pooled connection** → سيكون قيمة `DATABASE_URL`
   - **Direct connection** (بدون `-pooler`) → سيكون قيمة `DATABASE_URL_UNPOOLED`

> النتيجة: قاعدة بيانات فاضية، منفصلة تماماً عن العيادة الأولى.

---

## 3️⃣ مشروع Vercel جديد

1. ادخل **https://vercel.com** → **Add New… → Project**
2. اختر الريبو الجديد الذي أنشأته في الخطوة 1
3. قبل الضغط على Deploy، افتح **Environment Variables** وأضف:

| المتغيّر | القيمة |
|---|---|
| `DATABASE_URL` | رابط Neon **Pooled** |
| `DATABASE_URL_UNPOOLED` | رابط Neon **Direct** |
| `AUTH_SECRET` | نص عشوائي طويل (ولّده بـ `openssl rand -base64 32`) |
| `CLINIC_NAME` | اسم العيادة الجديدة |
| `CLINIC_PHONE` | تليفون العيادة (اختياري) |
| `ADMIN_EMAIL` | إيميل مدير العيادة الجديدة |
| `ADMIN_PASSWORD` | كلمة مرور قوية للمدير |
| `ADMIN_NAME` | اسم المدير |
| `SEED_DEMO` | `false` (عشان تبدأ نظيفة بدون بيانات وهمية) |
| `GEMINI_API_KEY` | (اختياري) لتفعيل المساعد الذكي |

4. اضغط **Deploy**.

> أمر البناء (`vercel.json`) يطبّق الهجرات تلقائياً. أما إنشاء حساب المدير فيتم بتشغيل seed مرة واحدة (الخطوة 4).

---

## 4️⃣ إنشاء حساب المدير (مرة واحدة)

بعد أول نشر ناجح، شغّل seed لإنشاء حساب المدير + اسم العيادة + الإجراءات الافتراضية.
أسهل طريقة من جهازك (مع نفس متغيّرات البيئة):

```bash
# في مجلد المشروع، مع ضبط DATABASE_URL و ADMIN_EMAIL ... إلخ في ملف .env
npm install
npx prisma migrate deploy
npx tsx prisma/seed.ts
```

> أو فعّل تشغيل seed داخل أمر البناء مؤقتاً، ثم أوقفه.

---

## ✅ جاهزة

ادخل على رابط الموقع الجديد → سجّل دخول بـ `ADMIN_EMAIL` / `ADMIN_PASSWORD`.
عيادة جديدة بالكامل، ببياناتها المنفصلة، بنفس كل المميزات.

> 🔒 **مهم:** كل عيادة لازم يكون لها `DATABASE_URL` و `AUTH_SECRET` **مختلفين**. لو شاركوا نفس قاعدة البيانات، البيانات هتختلط.
